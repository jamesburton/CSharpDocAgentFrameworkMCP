---
phase: 30-mcp-integration-and-incremental-ingestion
plan: 30-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DocAgent.McpServer/Config/DocAgentServerOptions.cs
  - src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs
  - src/DocAgent.McpServer/Tools/IngestionTools.cs
  - tests/DocAgent.Tests/TypeScriptIngestionServiceTests.cs
autonomous: true
requirements:
  - MCPI-01
  - MCPI-03

must_haves:
  truths:
    - "ingest_typescript tool is available via MCP"
    - "ingest_typescript enforces PathAllowlist"
    - "TypeScriptIngestionService reuses previous snapshot if no .ts/.tsx files changed"
    - "File manifest is saved in artifacts directory"
  artifacts:
    - path: "src/DocAgent.McpServer/Tools/IngestionTools.cs"
      provides: "ingest_typescript MCP tool handler"
    - path: "src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs"
      provides: "Incremental ingestion logic"
---

<objective>
Enable the ingest_typescript MCP tool and implement SHA-256 based incremental ingestion to avoid redundant sidecar runs.
</objective>

<execution_context>
@C:/Users/james/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-symbol-extraction/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Options and Service Logic</name>
  <files>
    src/DocAgent.McpServer/Config/DocAgentServerOptions.cs,
    src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs
  </files>
  <action>
    1. Add `TypeScriptFileExtensions` to `DocAgentServerOptions` (default: `.ts,.tsx`).
    2. In `TypeScriptIngestionService.IngestTypeScriptAsync`, implement incremental check:
       - Use `FileHasher` to scan the `tsconfig.json` directory for matching files.
       - Use a separate manifest file for TypeScript if needed, or share the existing one (but watch for path collisions). Prefer `ts-file-hashes.json`.
       - Compare current manifest against the one in `SnapshotStore` or artifacts dir.
       - If identical, return existing result.
       - If changed, run sidecar, then save new manifest.
  </action>
  <verify>
    Verify that subsequent calls to IngestTypeScriptAsync return quickly if no files changed.
  </verify>
  <done>
    Incremental logic is implemented and avoids redundant sidecar runs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ingest_typescript MCP Tool</name>
  <files>src/DocAgent.McpServer/Tools/IngestionTools.cs</files>
  <action>
    Add `IngestTypeScript` method to `IngestionTools.cs`:
    - Name: `ingest_typescript`
    - Description: "Ingest a TypeScript project (tsconfig.json), building a queryable symbol graph."
    - Parameter: `path` (string, Absolute path to tsconfig.json)
    - Enforce `PathAllowlist` on the input path.
    - Delegate to `_typeScriptIngestionService.IngestTypeScriptAsync`.
    - Return JSON response with `snapshotId`, `symbolCount`, `durationMs`.
  </action>
  <verify>
    Check that the tool is listed when querying the server for available tools.
  </verify>
  <done>
    `ingest_typescript` tool is functional and secure.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit Testing</name>
  <files>tests/DocAgent.Tests/TypeScriptIngestionServiceTests.cs</files>
  <action>
    Add tests to verify:
    - Incremental hit (no sidecar spawn).
    - Incremental miss (sidecar spawned).
    - PathAllowlist enforcement (via Tool tests if applicable, or integration tests).
  </action>
  <verify>
    `dotnet test` passes.
  </verify>
  <done>
    Unit tests confirm incremental logic correctness.
  </done>
</task>

</tasks>

<verification>
- `dotnet test` passes.
- `ingest_typescript` tool appears in MCP tools list.
</verification>

<success_criteria>
- ingest_typescript tool implemented.
- Incremental re-ingestion works for TypeScript projects.
</success_criteria>

<output>
After completion, create `.planning/phases/30-mcp-integration-and-incremental-ingestion/30-01-SUMMARY.md`
</output>
