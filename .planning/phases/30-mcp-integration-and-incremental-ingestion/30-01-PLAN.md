---
phase: 30-mcp-integration-and-incremental-ingestion
plan: 30-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DocAgent.McpServer/Config/DocAgentServerOptions.cs
  - src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs
  - src/DocAgent.McpServer/Tools/IngestionTools.cs
  - tests/DocAgent.Tests/TypeScriptIngestionServiceTests.cs
autonomous: true
requirements:
  - MCPI-01
  - MCPI-03

must_haves:
  truths:
    - "ingest_typescript tool is available via MCP with forceReindex parameter"
    - "ingest_typescript enforces PathAllowlist"
    - "Re-ingesting an unchanged TypeScript project returns skipped: true within 100ms"
    - "ingest_typescript_workspace discovers tsconfig.json files and ingests each as a separate project"
    - "Structured error responses use ErrorJson with categories: sidecar_timeout, parse_error, tsconfig_invalid"
  artifacts:
    - path: "src/DocAgent.McpServer/Tools/IngestionTools.cs"
      provides: "ingest_typescript and ingest_typescript_workspace MCP tool handlers"
    - path: "src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs"
      provides: "Incremental ingestion logic with manifest hashing"
    - path: "src/DocAgent.McpServer/Config/DocAgentServerOptions.cs"
      provides: "TypeScriptSidecarTimeoutSeconds option"
  key_links:
    - from: "src/DocAgent.McpServer/Tools/IngestionTools.cs"
      to: "src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs"
      via: "IngestTypeScriptAsync call with forceReindex parameter"
      pattern: "_typeScriptIngestionService\\.IngestTypeScriptAsync"
    - from: "src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs"
      to: "src/DocAgent.McpServer/Config/DocAgentServerOptions.cs"
      via: "TypeScriptSidecarTimeoutSeconds option injection"
      pattern: "TypeScriptSidecarTimeoutSeconds"
    - from: "src/DocAgent.McpServer/Tools/IngestionTools.cs"
      to: "MCP progress notifications"
      via: "progressToken extraction and callback wiring"
      pattern: "progressToken.*Meta"
---

<objective>
Enable the ingest_typescript MCP tool with forceReindex support, implement SHA-256 based incremental ingestion, add the ingest_typescript_workspace tool for monorepo support, and wire structured error responses and progress notifications.

Purpose: Make TypeScript projects ingestible via MCP with incremental caching and monorepo support.
Output: Working ingest_typescript and ingest_typescript_workspace tools with incremental detection.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-symbol-extraction/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Options and Incremental Manifest Logic</name>
  <files>
    src/DocAgent.McpServer/Config/DocAgentServerOptions.cs,
    src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs
  </files>
  <action>
    1. Add `TypeScriptSidecarTimeoutSeconds` to `DocAgentServerOptions` (int, default: 120). The `TypeScriptFileExtensions` option already exists.
    2. In `TypeScriptIngestionService.IngestTypeScriptAsync`, add `bool forceReindex = false` parameter and implement incremental check:
       - Use `FileHasher` to scan the `tsconfig.json` directory for matching file extensions.
       - Compute manifest that includes: SHA-256 hashes of all matched source files, the `tsconfig.json` file hash, and `package-lock.json` hash (if present in the same directory).
       - Store manifest as `ts-file-hashes.json` in the artifacts directory.
       - Compare current manifest against stored one. If identical AND `forceReindex` is false, return cached result with `skipped: true` and `reason: "no changes detected"` in the response envelope.
       - If changed (or forceReindex is true), run sidecar, then save new manifest.
    3. Use the `TypeScriptSidecarTimeoutSeconds` option to set a `CancellationTokenSource` timeout when spawning the sidecar process.
    4. On sidecar errors, return structured error responses using the `ErrorJson` helper with diagnostic categories:
       - `sidecar_timeout` — when the sidecar exceeds the configured timeout
       - `parse_error` — when the sidecar returns a parse failure
       - `tsconfig_invalid` — when the tsconfig.json is missing or malformed
  </action>
  <verify>
    <automated>dotnet build src/DocAgent.McpServer</automated>
  </verify>
  <done>
    Incremental logic is implemented with tsconfig.json + package-lock.json in manifest scope, configurable timeout, and structured error categories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement ingest_typescript MCP Tool with forceReindex and Progress</name>
  <files>src/DocAgent.McpServer/Tools/IngestionTools.cs</files>
  <action>
    Update the existing `IngestTypeScript` method in `IngestionTools.cs`:
    - Add `forceReindex` parameter: `[Description("Force re-index even if snapshot is current")] bool forceReindex = false` — matches `ingest_project` API shape per user decision.
    - Pass `forceReindex` through to `_typeScriptIngestionService.IngestTypeScriptAsync`.
    - Wire MCP progress notifications using the same pattern as `ingest_solution`:
      Extract `progressToken` from `requestContext.Params.Meta`, create `Func<int, int, string, Task>` callback, pass to service. Phases: "starting", "extracting", "indexing".
    - Update the JSON response envelope to include `skipped` (bool) and `reason` (string?) fields alongside existing `snapshotId`, `symbolCount`, `durationMs`, `warnings`, `indexError`.
    - Update the catch block to use structured `ErrorJson` categories (`sidecar_timeout`, `parse_error`, `tsconfig_invalid`) instead of generic `ingestion_failed` where applicable.
  </action>
  <verify>
    <automated>dotnet build src/DocAgent.McpServer</automated>
  </verify>
  <done>
    `ingest_typescript` tool has forceReindex parameter, sends progress notifications, returns skipped/reason fields, and uses structured error categories.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit Testing for Incremental Logic</name>
  <files>tests/DocAgent.Tests/TypeScriptIngestionServiceTests.cs</files>
  <behavior>
    - Test 1: Incremental hit — second call with no file changes returns skipped: true, no sidecar spawn.
    - Test 2: Incremental miss — modifying a .ts file triggers sidecar re-run.
    - Test 3: forceReindex=true bypasses incremental check even when manifest matches.
    - Test 4: tsconfig.json change triggers re-ingestion.
    - Test 5: package-lock.json change triggers re-ingestion.
    - Test 6: PathAllowlist enforcement (via tool-level tests if applicable).
  </behavior>
  <action>
    Add tests to verify incremental logic correctness including the manifest scope for tsconfig.json and package-lock.json hashes, and the forceReindex bypass. Use the `PipelineOverride` test seam from Phase 28 to inject mock sidecar output.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~TypeScriptIngestionServiceTests"</automated>
  </verify>
  <done>
    Unit tests confirm incremental logic, forceReindex bypass, and manifest scope correctness.
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement ingest_typescript_workspace MCP Tool</name>
  <files>
    src/DocAgent.McpServer/Tools/IngestionTools.cs,
    src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs
  </files>
  <action>
    1. Add `IngestTypeScriptWorkspaceAsync` method to `TypeScriptIngestionService`:
       - Accept a root directory path.
       - Discover all `tsconfig.json` files under the root (excluding `node_modules` directories).
       - For each discovered tsconfig.json, call `IngestTypeScriptAsync` (reusing incremental logic).
       - Return a per-project summary with: tsconfig path, snapshotId, symbolCount, status (success/skipped/error), reason.
    2. Add `IngestTypeScriptWorkspace` tool method to `IngestionTools.cs`:
       - Name: `ingest_typescript_workspace`
       - Description: "Ingest a TypeScript workspace (monorepo), discovering and ingesting all tsconfig.json projects under a root directory."
       - Parameters: `path` (string, required — root directory), `forceReindex` (bool, default false)
       - Enforce `PathAllowlist` on the root path.
       - Wire MCP progress notifications (per-project progress: "ingesting project 1/N: {tsconfig path}").
       - Return JSON response paralleling `ingest_solution` shape: total project count, ingested count, per-project results, total duration, warnings.
       - Use structured `ErrorJson` for errors.
    3. This tool parallels `ingest_solution` for .NET — discover projects, ingest each, return per-project summary.
  </action>
  <verify>
    <automated>dotnet build src/DocAgent.McpServer</automated>
  </verify>
  <done>
    `ingest_typescript_workspace` tool discovers tsconfig.json files under a root directory, ingests each as a separate project, and returns per-project summary. Tool is registered and available via MCP.
  </done>
</task>

</tasks>

<verification>
- `dotnet test` passes.
- `ingest_typescript` tool appears in MCP tools list with `forceReindex` parameter.
- `ingest_typescript_workspace` tool appears in MCP tools list.
- Incremental skip returns `skipped: true` and `reason: "no changes detected"`.
</verification>

<success_criteria>
- ingest_typescript tool implemented with forceReindex, progress notifications, and structured errors.
- ingest_typescript_workspace tool implemented for monorepo support.
- Incremental re-ingestion works with manifest covering source files + tsconfig.json + package-lock.json.
- Configurable sidecar timeout via TypeScriptSidecarTimeoutSeconds (default 120s).
</success_criteria>

<output>
After completion, create `.planning/phases/30-mcp-integration-and-incremental-ingestion/30-01-SUMMARY.md`
</output>
