---
phase: 31-verification-and-hardening
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - tests/DocAgent.Tests/TypeScriptRobustnessTests.cs
  - src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs
  - src/ts-symbol-extractor/src/index.ts
  - docs/Architecture.md
  - docs/Plan.md
autonomous: true
requirements: [VERF-03]
must_haves:
  truths:
    - "Ingestion fails with a clear error for missing or invalid tsconfig.json"
    - "TypeScript files with syntax errors are still partially ingested (best effort)"
    - "PathAllowlist prevents ingestion of files outside the allowed directory"
    - "No absolute paths are leaked in SymbolNode.Span fields"
    - "All debug logs are removed from production-ready sidecar and service"
  artifacts:
    - path: "tests/DocAgent.Tests/TypeScriptRobustnessTests.cs"
      provides: "Negative tests for error conditions"
    - path: "docs/Architecture.md"
      provides: "Updated architecture documentation with TS details"
  key_links:
    - from: "src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs"
      to: "src/DocAgent.Core/IngestionMetadata.cs"
      via: "PathAllowlist check"
---

<objective>
Harden the TypeScript ingestion pipeline by adding robust error handling, security verification, and cleaning up debug artifacts.

Purpose: Ensure the pipeline is production-ready for v2.0.
Output: Negative tests, security validation, and updated documentation.
</objective>

<execution_context>
@C:/Users/james/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs
@src/ts-symbol-extractor/src/index.ts
@docs/Architecture.md
@docs/Plan.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Negative Testing and Robustness</name>
  <files>tests/DocAgent.Tests/TypeScriptRobustnessTests.cs</files>
  <action>
    Create a new test class `TypeScriptRobustnessTests.cs` and implement negative test cases for the TypeScript pipeline:
    - Missing `tsconfig.json`: Verify `IngestTypeScriptAsync` throws or returns a clear error if the file does not exist.
    - Invalid `tsconfig.json` (malformed JSON): Verify the error is caught and reported gracefully.
    - TypeScript files with syntax errors: Create a TS file with deliberate syntax errors and verify that the extractor still extracts whatever valid symbols it can (best effort) rather than crashing.
    - Node.js missing: Use a mock or temporary PATH modification to trigger `NodeAvailabilityValidator` failure and verify the user gets a helpful installation instruction.
  </action>
  <verify>
    Run `dotnet test --filter TypeScriptRobustnessTests` and confirm all negative scenarios are handled correctly.
  </verify>
  <done>
    All negative test cases pass and error handling is robust.
  </done>
</task>

<task type="auto">
  <name>Task 2: Security Hardening and Path Leak Prevention</name>
  <files>src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs</files>
  <action>
    Implement security-focused tests to verify:
    - `PathAllowlist` enforcement in `ingest_typescript`: Confirm that trying to ingest a `tsconfig.json` outside the allowed root (e.g., `../../secrets.json`) is blocked.
    - Path Anonymization: Verify that all `SymbolNode.Span.FilePath` values in the resulting snapshot are relative to the project root and do not leak absolute machine paths (e.g., `C:\Users\James\Code\file.ts` -> `file.ts`).
    - Audit Logging: Verify that `AuditLogger` records a `TypeScriptIngested` event with correct metadata (elapsed time, file count).
  </action>
  <verify>
    Review the code in `TypeScriptIngestionService.cs` and run the security tests.
  </verify>
  <done>
    Security validation confirmed and no absolute path leaks detected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final Cleanup and Documentation Refresh</name>
  <files>src/ts-symbol-extractor/src/index.ts,docs/Architecture.md,docs/Plan.md</files>
  <action>
    Perform final cleanup of the v2.0 codebase:
    - Remove all debug logs (`console.log`, `Console.WriteLine`) from the sidecar and C# service that were used during development. Ensure the sidecar only outputs the final NDJSON snapshot on stdout.
    - Update `docs/Architecture.md` to include the Node.js sidecar architecture, the NDJSON protocol definition, and the TypeScript symbol mapping strategy.
    - Update `docs/Plan.md` with the final status of v2.0.
    - Conduct a final "sniff test" of the TypeScript integration to ensure it follows the project's coding conventions.
  </action>
  <verify>
    Check for console logs using `grep` and review the updated markdown documentation.
  </verify>
  <done>
    Codebase cleaned up and documentation updated for v2.0 release.
  </done>
</task>

</tasks>

<verification>
1. Negative tests confirm graceful failure for all tested error conditions.
2. Security tests confirm PathAllowlist enforcement and path relativeization.
3. No debug logs remain in the source code.
4. Documentation accurately reflects the TypeScript implementation.
</verification>

<success_criteria>
1. `TypeScriptRobustnessTests` passes with 100% success rate.
2. Security audit confirms zero absolute path leaks in snapshots.
3. `docs/Architecture.md` contains the TypeScript sidecar section.
</success_criteria>

<output>
After completion, create `.planning/phases/31-verification-and-hardening/31-02-SUMMARY.md`
</output>
