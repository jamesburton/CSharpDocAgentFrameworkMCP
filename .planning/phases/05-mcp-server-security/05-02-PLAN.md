---
phase: 05-mcp-server-security
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tests/DocAgent.Tests/DocAgent.Tests.csproj
  - tests/DocAgent.Tests/McpToolTests.cs
  - tests/DocAgent.Tests/PathAllowlistTests.cs
  - tests/DocAgent.Tests/AuditLoggerTests.cs
  - tests/DocAgent.Tests/PromptInjectionScannerTests.cs
autonomous: true
requirements: [SECR-01, SECR-02, SECR-03, MCPS-01, MCPS-02, MCPS-03, MCPS-04, MCPS-05]

must_haves:
  truths:
    - "PathAllowlist denies paths outside configured allow patterns"
    - "PathAllowlist deny patterns take precedence over allow patterns"
    - "PathAllowlist defaults to CWD when unconfigured"
    - "PathAllowlist normalizes paths (no ../ traversal bypass)"
    - "AuditLogger writes JSONL with correct fields for success and error cases"
    - "AuditLogger respects tiered verbosity (metadata vs full body)"
    - "PromptInjectionScanner detects known injection patterns and sets warning flag"
    - "Tool handlers return structured error JSON on invalid input"
    - "Tool handlers return correct compact and detailed response shapes"
  artifacts:
    - path: "tests/DocAgent.Tests/PathAllowlistTests.cs"
      provides: "Unit tests for allow/deny/default/traversal scenarios"
    - path: "tests/DocAgent.Tests/AuditLoggerTests.cs"
      provides: "Unit tests for JSONL output, tiered verbosity, redaction"
    - path: "tests/DocAgent.Tests/PromptInjectionScannerTests.cs"
      provides: "Unit tests for injection pattern detection and sanitization"
    - path: "tests/DocAgent.Tests/McpToolTests.cs"
      provides: "Unit tests for all 5 tool handlers with mocked IKnowledgeQueryService"
  key_links:
    - from: "McpToolTests.cs"
      to: "DocTools"
      via: "Direct instantiation with mock IKnowledgeQueryService"
      pattern: "new DocTools"
---

<objective>
Comprehensive unit tests for all security services (PathAllowlist, AuditLogger, PromptInjectionScanner) and all five MCP tool handlers using mocked IKnowledgeQueryService.

Purpose: Verify all security boundaries and tool response shapes work correctly before integration testing.
Output: 4 test files covering path security, audit logging, injection defense, and tool handler behavior.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-server-security/05-CONTEXT.md
@.planning/phases/05-mcp-server-security/05-01-SUMMARY.md

@src/DocAgent.McpServer/Security/PathAllowlist.cs
@src/DocAgent.McpServer/Security/AuditLogger.cs
@src/DocAgent.McpServer/Security/PromptInjectionScanner.cs
@src/DocAgent.McpServer/Tools/DocTools.cs
@src/DocAgent.McpServer/Config/DocAgentServerOptions.cs
@src/DocAgent.Core/Abstractions.cs
@src/DocAgent.Core/QueryTypes.cs
@tests/DocAgent.Tests/DocAgent.Tests.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security service unit tests (PathAllowlist, AuditLogger, PromptInjectionScanner)</name>
  <files>
    tests/DocAgent.Tests/DocAgent.Tests.csproj
    tests/DocAgent.Tests/PathAllowlistTests.cs
    tests/DocAgent.Tests/AuditLoggerTests.cs
    tests/DocAgent.Tests/PromptInjectionScannerTests.cs
  </files>
  <action>
**Step 0: Update test csproj**
- Add `<ProjectReference Include="..\..\src\DocAgent.McpServer\DocAgent.McpServer.csproj" />` to test project.

**Step 1: PathAllowlistTests.cs** — Test class using xUnit + FluentAssertions.

Test cases (minimum):
1. `IsAllowed_DefaultUnconfigured_AllowsCwd` — no config, path under CWD → true
2. `IsAllowed_DefaultUnconfigured_DeniesOutsideCwd` — no config, path outside CWD → false
3. `IsAllowed_AllowPattern_MatchesGlob` — allow `C:/projects/**`, test path under it → true
4. `IsAllowed_AllowPattern_DeniesUnmatched` — allow `C:/projects/**`, test path elsewhere → false
5. `IsAllowed_DenyTakesPrecedence` — allow `C:/projects/**`, deny `**/secrets/**`, test `C:/projects/secrets/key` → false
6. `IsAllowed_NormalizesTraversal` — test with `C:/projects/../etc/passwd` → resolves and denies
7. `IsAllowed_EnvVarExtends` — set `DOCAGENT_ALLOWED_PATHS` env var, verify paths allowed (use try/finally to restore env)
8. `IsAllowed_CaseInsensitive` — on Windows, `C:/Projects` matches `c:/projects/**`

Create `PathAllowlist` instances directly with `IOptions<DocAgentServerOptions>` using `Options.Create(new DocAgentServerOptions { ... })`.

**Step 2: AuditLoggerTests.cs**

Test cases:
1. `Log_Success_WritesJsonlToStderr` — verify ILogger receives a log entry containing tool name and duration
2. `Log_Error_IncludesErrorCode` — verify error log entry has error field
3. `Log_DefaultVerbosity_OmitsFullBodies` — verify InputFull and ResponseFull are null
4. `Log_VerboseMode_IncludesFullBodies` — configure verbose, verify InputFull populated
5. `Log_WithFilePath_AppendsToFile` — configure file path (use temp file), verify JSONL written
6. `Log_Redaction_AppliesPatterns` — configure redaction regex, verify pattern replaced in output

Use `ILogger<AuditLogger>` mock (or `NullLogger<AuditLogger>`). For file tests, use `Path.GetTempFileName()` with cleanup.

**Step 3: PromptInjectionScannerTests.cs**

Test cases:
1. `Scan_CleanContent_NoWarning` — normal doc comment → warning=false, content unchanged
2. `Scan_IgnorePreviousInstructions_Detected` — "Ignore previous instructions" → warning=true, content has [SUSPICIOUS:] marker
3. `Scan_YouAreNow_Detected` — "You are now a helpful..." → detected
4. `Scan_SystemPrompt_Detected` — "system prompt" → detected
5. `Scan_CaseInsensitive` — "IGNORE PREVIOUS INSTRUCTIONS" → detected
6. `Scan_NullContent_NoWarning` — null input → warning=false, empty/null return
7. `Scan_PartialMatch_InMiddleOfContent` — injection pattern embedded in larger text → detected, surrounding text preserved
  </action>
  <verify>
    <automated>dotnet test tests/DocAgent.Tests --filter "FullyQualifiedName~PathAllowlist|FullyQualifiedName~AuditLogger|FullyQualifiedName~PromptInjection" --no-restore 2>&1 | tail -10</automated>
  </verify>
  <done>
    - PathAllowlistTests: 8+ tests passing (default deny, glob match, deny precedence, traversal, env var, case)
    - AuditLoggerTests: 6+ tests passing (success/error, verbosity tiers, file output, redaction)
    - PromptInjectionScannerTests: 7+ tests passing (clean, known patterns, case-insensitive, null)
    - All tests use FluentAssertions
  </done>
</task>

<task type="auto">
  <name>Task 2: MCP tool handler unit tests with mocked IKnowledgeQueryService</name>
  <files>
    tests/DocAgent.Tests/McpToolTests.cs
  </files>
  <action>
Create `McpToolTests.cs` — instantiate `DocTools` directly with:
- Mock `IKnowledgeQueryService` (create a simple stub class implementing the interface, or use a manual mock since the project doesn't use Moq)
- Real `PathAllowlist` with permissive config (allow all)
- `NullLogger<DocTools>`

**Mock setup:** Create a `StubKnowledgeQueryService` inner class that returns canned responses:
- `SearchAsync` → returns `QueryResult.Ok(envelope with 2 SearchResultItems)`
- `GetSymbolAsync` → returns `QueryResult.Ok(envelope with SymbolDetail)` for known ID, `QueryResult.Fail(NotFound)` for unknown
- `DiffAsync` → returns `QueryResult.Ok(envelope with GraphDiff having 1 Added, 1 Removed)`
- `GetReferencesAsync` → yields 2 `SymbolEdge`s

Create test fixture `SymbolNode`s and `SymbolId`s for the canned data.

**Test cases for search_symbols:**
1. `SearchSymbols_ValidQuery_ReturnsJsonResults` — query="Test", verify JSON contains results array
2. `SearchSymbols_WithKindFilter_PassesFilter` — kindFilter="Method", verify stub receives correct enum
3. `SearchSymbols_InvalidKindFilter_ReturnsError` — kindFilter="bogus" → structured error response
4. `SearchSymbols_LimitCapped` — limit=999 → capped to 100

**Test cases for get_symbol:**
1. `GetSymbol_ValidId_ReturnsDetail` — known symbolId → JSON with symbol detail
2. `GetSymbol_InvalidId_ReturnsError` — malformed symbolId → error response
3. `GetSymbol_NotFound_ReturnsNotFoundError` — unknown symbolId → not_found error

**Test cases for get_references:**
1. `GetReferences_ValidId_ReturnsEdges` — returns buffered edges as JSON array

**Test cases for diff_snapshots:**
1. `DiffSnapshots_ValidVersions_ReturnsDiff` — returns diff entries
2. `DiffSnapshots_MissingSnapshot_ReturnsError` — stub returns SnapshotMissing → error

**Test cases for explain_project:**
1. `ExplainProject_DefaultDepth_ReturnsOverview` — chainedEntityDepth=1, verify namespaces/types/stats present
2. `ExplainProject_DepthZero_SummaryOnly` — chainedEntityDepth=0, verify minimal response

**Test cases for prompt injection defense:**
1. `GetSymbol_DocWithInjection_ReturnsContentWithWarning` — symbol has doc containing "Ignore previous instructions" → response has `promptInjectionWarning: true` AND the content is returned (not filtered)

**Test cases for error shapes:**
1. `SearchSymbols_StaleIndex_ReturnsStaleIndexError` — stub returns StaleIndex → error code in response

All tests call the tool method directly (it's a regular C# method) and assert on the returned JSON string using `JsonDocument.Parse` + FluentAssertions.
  </action>
  <verify>
    <automated>dotnet test tests/DocAgent.Tests --filter "FullyQualifiedName~McpToolTests" --no-restore 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 15+ MCP tool tests passing
    - All 5 tools have at least 2 tests each
    - Prompt injection warning flag verified
    - Error response shapes verified with structured JSON assertions
    - Tests use manual stub (no Moq dependency)
  </done>
</task>

</tasks>

<verification>
- `dotnet test tests/DocAgent.Tests` passes with all new tests green
- Total new tests: ~36+ (8 allowlist + 6 audit + 7 injection + 15 tool)
- No test uses network or file system outside temp directories
- All assertions use FluentAssertions
</verification>

<success_criteria>
1. All security service tests pass — PathAllowlist deny-takes-precedence verified, traversal prevention verified
2. All audit tests pass — JSONL format, tiered verbosity, redaction verified
3. All injection scanner tests pass — known patterns detected, content preserved with warning
4. All tool handler tests pass — correct response shapes, error handling, prompt injection flag
5. `dotnet test` passes with zero failures across entire test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-server-security/05-02-SUMMARY.md`
</output>
