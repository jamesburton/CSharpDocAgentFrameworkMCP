---
phase: 29-core-symbol-extraction
plan: 29-02
type: execute
wave: 2
depends_on: [29-01]
files_modified: [src/ts-symbol-extractor/src/extractor.ts, src/ts-symbol-extractor/src/doc-extractor.ts]
autonomous: true
requirements: [EXTR-01, EXTR-04, EXTR-05, EXTR-06]

must_haves:
  truths:
    - "Classes, interfaces, enums, functions, and type aliases are correctly extracted"
    - "Contains edges correctly link members to their parent types or namespaces"
    - "Inherits and Implements edges accurately represent class/interface hierarchies"
    - "JSDoc documentation is extracted and parsed into the DocComment model"
    - "Accessibility correctly reflects export status (exported = Public)"
  artifacts:
    - path: "src/ts-symbol-extractor/src/extractor.ts"
      provides: "Full member and relationship extraction"
    - path: "src/ts-symbol-extractor/src/doc-extractor.ts"
      provides: "JSDoc/TSDoc parsing logic"
  key_links:
    - from: "src/ts-symbol-extractor/src/extractor.ts"
      to: "src/ts-symbol-extractor/src/doc-extractor.ts"
      via: "getDocComment call"
    - from: "src/ts-symbol-extractor/src/extractor.ts"
      to: "src/ts-symbol-extractor/src/types.ts"
      via: "SymbolKind and SymbolEdgeKind mapping"
---

<objective>
Complete the extraction of members (methods, properties, fields), relationships (inheritance, implementation, containment), and documentation (JSDoc/TSDoc) to produce a comprehensive symbol graph.
</objective>

<execution_context>
@C:/Users/james/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-core-symbol-extraction/29-01-SUMMARY.md
@src/ts-symbol-extractor/src/types.ts
@src/ts-symbol-extractor/src/extractor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detailed Declaration and Member Extraction</name>
  <files>src/ts-symbol-extractor/src/extractor.ts</files>
  <action>
    Extend the extractor to process declarations within source files and their members.
    1. Extract the following types into `SymbolNode`s:
       - `ts.SymbolFlags.Class` -> `SymbolKind.Type`
       - `ts.SymbolFlags.Interface` -> `SymbolKind.Type`
       - `ts.SymbolFlags.TypeAlias` -> `SymbolKind.Type`
       - `ts.SymbolFlags.RegularEnum` -> `SymbolKind.Type`
       - `ts.SymbolFlags.Function` -> `SymbolKind.Method`
    2. Extract members of these types:
       - `ts.SymbolFlags.Method` -> `SymbolKind.Method`
       - `ts.SymbolFlags.Property` -> `SymbolKind.Property`
       - `ts.SymbolFlags.EnumMember` -> `SymbolKind.EnumMember`
       - Constructors, fields.
    3. Map accessibility:
       - Exported from module -> `Accessibility.Public`.
       - Not exported -> `Accessibility.Internal`.
       - Handle `private`/`protected` modifiers on class members.
  </action>
  <verify>
    Update fixture projects with classes/members and run `npm test`. Verify golden JSON contains the new nodes.
  </verify>
  <done>
    All requested declaration types and their members are extracted with correct kind and accessibility.
  </done>
</task>

<task type="auto">
  <name>Task 2: Relationship Extraction (SymbolEdges)</name>
  <files>src/ts-symbol-extractor/src/extractor.ts</files>
  <action>
    Implement logic to generate `SymbolEdge` objects between nodes.
    1. `Contains`: Every member belongs to a parent type, every top-level declaration belongs to a namespace (file).
    2. `Inherits` (Extends): For classes extending other classes or interfaces extending other interfaces.
    3. `Implements`: For classes implementing interfaces.
    - Ensure all edges use deterministic `SymbolId`s for both source and target.
    - If a target is an external type (e.g., from `node_modules` or a library), handle it gracefully (e.g., generate a stable ID for the external symbol even if it's not fully extracted).
  </action>
  <verify>
    Run `npm test`. Verify golden JSON contains `Contains`, `Inherits`, and `Implements` edges connecting the correct nodes.
  </verify>
  <done>
    Complete relationship graph is generated for the project.
  </done>
</task>

<task type="auto">
  <name>Task 3: JSDoc/TSDoc Extraction</name>
  <files>src/ts-symbol-extractor/src/doc-extractor.ts, src/ts-symbol-extractor/src/extractor.ts</files>
  <action>
    Implement `getDocComment(symbol: ts.Symbol, checker: ts.TypeChecker): DocComment | null` in `doc-extractor.ts`.
    1. Extract main summary from `symbol.getDocumentationComment(checker)`.
    2. Extract tags using `ts.getJSDocTags(node)` or `symbol.getJsDocTags()`.
    3. Map tags to `DocComment` fields:
       - `@param` -> `params` dictionary.
       - `@returns` -> `returns` string.
       - `@example` -> `example` string.
       - `@remarks` -> `remarks` string.
       - `@see` -> `see` array.
       - `@throws` -> `throws` dictionary.
    4. Integrate into `extractor.ts` to populate the `docComment` field of `SymbolNode`.
  </action>
  <verify>
    Add JSDoc to fixture project and verify it appears in the golden JSON output.
  </verify>
  <done>
    Structured documentation is extracted for all supported symbol types.
  </done>
</task>

</tasks>

<verification>
### Automated Verification
- `npm run build` succeeds.
- `npm test` passes all tests with updated golden files reflecting members, edges, and documentation.
- All 8 requirements (EXTR-01 to EXTR-08) are addressed by the combined output of 29-01 and 29-02.

### Manual Verification
- Review extraction output for a representative project (e.g., a sample with inheritance and JSDoc) to ensure correctness.
</verification>

<success_criteria>
- Snapshot contains a complete symbol graph including members and relationships.
- JSDoc is correctly parsed into structured fields.
- Accessibility and SymbolKind mapping follow established conventions.
- All extracted symbols have stable IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/29-core-symbol-extraction/29-02-SUMMARY.md`
</output>
