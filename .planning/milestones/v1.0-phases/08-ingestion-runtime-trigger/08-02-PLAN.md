---
phase: 08-ingestion-runtime-trigger
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - tests/DocAgent.Tests/IngestAndQueryE2ETests.cs
autonomous: true
requirements: [INGS-06]

must_haves:
  truths:
    - "E2E test: ingest_project tool call → discover → parse → snapshot → index → search_symbols returns results in a single session"
    - "ingest_project respects PathAllowlist — denied path returns structured error"
    - "After ingestion completes, search_symbols finds symbols from the ingested project"
  artifacts:
    - tests/DocAgent.Tests/IngestAndQueryE2ETests.cs
  key_links:
    - "E2E test builds real DI container with AddDocAgent(), invokes IngestionService.IngestAsync, then queries via KnowledgeQueryService.SearchAsync"
    - "PathAllowlist configured to allow temp test directory, deny everything else"
---

<objective>
E2E integration test proving the full pipeline works end-to-end: ingest a real .NET project, then query it via KnowledgeQueryService and confirm symbols are found.

Purpose: Validate the entire pipeline (discover → parse → snapshot → index → query) works as a connected system, not just in isolation. This is the final proof that INGS-06 is satisfied.
Output: IngestAndQueryE2ETests.cs with Category=Integration trait.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ingestion-runtime-trigger/08-RESEARCH.md
@.planning/phases/08-ingestion-runtime-trigger/08-01-SUMMARY.md

<interfaces>
<!-- From 08-01 plan outputs -->

From src/DocAgent.McpServer/Ingestion/IIngestionService.cs:
```csharp
public interface IIngestionService
{
    Task<IngestionResult> IngestAsync(
        string path, string? includeGlob, string? excludeGlob,
        bool forceReindex, Func<int, int, string, Task>? reportProgress,
        CancellationToken cancellationToken);
}
```

From src/DocAgent.McpServer/Ingestion/IngestionResult.cs:
```csharp
public sealed record IngestionResult(
    string SnapshotId, int SymbolCount, int ProjectCount,
    TimeSpan Duration, IReadOnlyList<string> Warnings, string? IndexError = null);
```

From src/DocAgent.McpServer/ServiceCollectionExtensions.cs:
```csharp
services.AddSingleton<IIngestionService, IngestionService>();
```

From src/DocAgent.Core/Abstractions.cs:
```csharp
public interface IKnowledgeQueryService
{
    IAsyncEnumerable<SearchResultItem> SearchAsync(string query, SearchOptions? options = null, CancellationToken ct = default);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E integration test — ingest real project then query symbols</name>
  <files>
    tests/DocAgent.Tests/IngestAndQueryE2ETests.cs
  </files>
  <action>
**Create `tests/DocAgent.Tests/IngestAndQueryE2ETests.cs`:**

Mark with `[Trait("Category", "Integration")]` for CI filter separation (same convention as Phase 05-03 integration tests).

**Test 1: `IngestProject_ThenSearchSymbols_ReturnsResults`**
1. Create a temp directory with a minimal .csproj and a .cs file containing a public class with XML doc comment (e.g., `/// <summary>Test class</summary> public class HelloWorld { public void SayHello() {} }`).
2. Write a minimal .csproj file targeting net10.0 with `<GenerateDocumentationFile>true</GenerateDocumentationFile>`.
3. Build real DI container: `new ServiceCollection()`, call `AddDocAgent()` with `ArtifactsDir` set to a temp directory, add `IOptions<DocAgentServerOptions>` with the temp project path in AllowedPaths.
4. Resolve `IIngestionService` from the container.
5. Call `IngestAsync(csprojPath, null, null, false, null, CancellationToken.None)`.
6. Assert result: `SymbolCount > 0`, `ProjectCount == 1`, `IndexError` is null.
7. Resolve `IKnowledgeQueryService` from the container (scoped — create a scope first).
8. Call `SearchAsync("HelloWorld")` and collect results.
9. Assert at least one result contains "HelloWorld" in its symbol name.
10. Clean up temp directories in `Dispose`.

**Test 2: `IngestProject_PathDenied_ThrowsOrReturnsError`**
1. Configure PathAllowlist with empty AllowedPaths (default-deny).
2. Attempt to ingest a path outside the allowed list.
3. This test validates PathAllowlist at the tool level — but since we're testing IngestionService directly (not the tool), this test should instead verify the tool-level behavior. If testing at service level, skip this (already covered in 08-01 IngestionToolTests). Alternatively, test the full IngestionTools class with a real PathAllowlist.

**Test 3: `IngestProject_ThenSearchByDocComment_ReturnsResults`**
1. Same setup as Test 1, but search for a term from the XML doc comment (e.g., "Test class").
2. Assert the BM25 index returns the symbol matching the doc text search.

**Important implementation notes:**
- MSBuildLocator must be registered before any Roslyn workspace use. Call `Microsoft.Build.Locator.MSBuildLocator.RegisterDefaults()` in a static constructor or `[AssemblyFixture]` — check existing test patterns (Phase 2 tests likely already handle this).
- Use `IDisposable` or `IAsyncLifetime` to clean up temp dirs and dispose BM25SearchIndex.
- Keep test projects minimal (single file, no dependencies) to keep test execution under 30 seconds.
- The test needs project references to `DocAgent.McpServer`, `DocAgent.Ingestion`, `DocAgent.Indexing`, `DocAgent.Core` — check that `DocAgent.Tests.csproj` already has these (it should from prior phases).
  </action>
  <verify>
    <automated>dotnet test --filter "Category=Integration&FullyQualifiedName~IngestAndQueryE2ETests" -v n</automated>
  </verify>
  <done>
    - E2E test ingests a real minimal .NET project and queries symbols successfully
    - Search by symbol name ("HelloWorld") returns matching results
    - Search by doc comment text returns matching results
    - All existing tests still pass (`dotnet test`)
    - INGS-06 requirement fully satisfied: runtime ingestion trigger works end-to-end
  </done>
</task>

</tasks>

<verification>
1. `dotnet test --filter "Category=Integration&FullyQualifiedName~IngestAndQueryE2ETests"` — all E2E tests pass
2. `dotnet test` — full suite passes (no regressions)
3. Verify INGS-06 success criteria from ROADMAP:
   - [x] ingest_project MCP tool accepts a solution/project path and triggers full pipeline
   - [x] After ingestion, new snapshot is immediately queryable via existing MCP tools
   - [x] Ingestion trigger respects PathAllowlist security boundary
   - [x] E2E test: tool call → discover → parse → snapshot → index → query succeeds
</verification>

<success_criteria>
- E2E integration test passes: ingest → search returns real symbols
- Full test suite passes with no regressions
- Phase 8 complete: INGS-06 fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/08-ingestion-runtime-trigger/08-02-SUMMARY.md`
</output>
