---
phase: 13-core-domain-extensions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DocAgent.Core/SolutionTypes.cs
  - tests/DocAgent.Tests/SolutionSnapshotTests.cs
autonomous: true
requirements: [GRAPH-01, GRAPH-03]

must_haves:
  truths:
    - "SolutionSnapshot holds per-project SymbolGraphSnapshots with solution metadata"
    - "ProjectEntry records carry name, path, and dependency references"
    - "ProjectEdge collection models the project dependency DAG"
    - "SolutionSnapshot round-trips through MessagePack serialization"
  artifacts:
    - path: "src/DocAgent.Core/SolutionTypes.cs"
      provides: "SolutionSnapshot, ProjectEntry, ProjectEdge records"
      contains: "record SolutionSnapshot"
    - path: "tests/DocAgent.Tests/SolutionSnapshotTests.cs"
      provides: "Unit tests for new solution types and serialization"
      contains: "SolutionSnapshotTests"
  key_links:
    - from: "src/DocAgent.Core/SolutionTypes.cs"
      to: "src/DocAgent.Core/Symbols.cs"
      via: "references SymbolGraphSnapshot"
      pattern: "IReadOnlyList<SymbolGraphSnapshot>"
---

<objective>
Create new solution-level aggregate types: SolutionSnapshot, ProjectEntry, and ProjectEdge.

Purpose: Provide the top-level container that holds per-project snapshots and the project dependency DAG, enabling Phase 14 (ingestion) and Phase 16 (solution MCP tools) to work with solution-level data.
Output: New SolutionTypes.cs with three record types; SolutionSnapshotTests.cs with construction, equality, and MessagePack roundtrip tests.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-core-domain-extensions/13-RESEARCH.md

@src/DocAgent.Core/Symbols.cs

<interfaces>
<!-- Types from Symbols.cs that SolutionSnapshot references -->

From src/DocAgent.Core/Symbols.cs:
```csharp
public sealed record SymbolGraphSnapshot(
    string SchemaVersion, string ProjectName, string SourceFingerprint, string? ContentHash,
    DateTimeOffset CreatedAt, IReadOnlyList<SymbolNode> Nodes, IReadOnlyList<SymbolEdge> Edges,
    IngestionMetadata? IngestionMetadata = null);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SolutionTypes.cs with SolutionSnapshot, ProjectEntry, ProjectEdge</name>
  <files>src/DocAgent.Core/SolutionTypes.cs</files>
  <action>
Create a NEW file `src/DocAgent.Core/SolutionTypes.cs` in the `DocAgent.Core` namespace with:

1. `ProjectEntry` sealed record:
   - `string Name` — project name (matches SymbolGraphSnapshot.ProjectName)
   - `string Path` — relative path to .csproj
   - `IReadOnlyList<string> DependsOn` — project names this project references

2. `ProjectEdge` sealed record:
   - `string From` — source project name
   - `string To` — target project name

3. `SolutionSnapshot` sealed record:
   - `string? SolutionName` — nullable for backward compat
   - `IReadOnlyList<ProjectEntry> Projects` — project metadata entries
   - `IReadOnlyList<ProjectEdge> ProjectDependencies` — the project DAG edges
   - `IReadOnlyList<SymbolGraphSnapshot> ProjectSnapshots` — per-project symbol graphs
   - `DateTimeOffset CreatedAt` — when this solution snapshot was built

All types must be serializable with ContractlessStandardResolver (use records with IReadOnlyList, string, DateTimeOffset — all supported). No abstract bases, no polymorphism.
  </action>
  <verify>dotnet build src/DocAgent.Core -q</verify>
  <done>SolutionTypes.cs compiles cleanly. Three new records exported from DocAgent.Core namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Add SolutionSnapshotTests with construction, equality, and MessagePack roundtrip</name>
  <files>tests/DocAgent.Tests/SolutionSnapshotTests.cs</files>
  <action>
Create `tests/DocAgent.Tests/SolutionSnapshotTests.cs` with these test methods:

1. `ProjectEntry_Construction_And_Equality`: Create two identical ProjectEntry records, assert they are equal (record structural equality). Create one with different DependsOn, assert not equal.

2. `ProjectEdge_Construction_And_Equality`: Same pattern for ProjectEdge.

3. `SolutionSnapshot_Construction_With_Empty_Projects`: Create a SolutionSnapshot with empty Projects, ProjectDependencies, ProjectSnapshots lists. Assert all properties accessible and correct.

4. `SolutionSnapshot_With_Multiple_Projects`: Create a SolutionSnapshot with 2 ProjectEntry items, 1 ProjectEdge (A depends on B), 2 SymbolGraphSnapshots. Assert Projects.Count == 2, ProjectDependencies.Count == 1.

5. `SolutionSnapshot_MessagePack_Roundtrip`: Create a full SolutionSnapshot with projects, edges, and snapshots containing nodes. Serialize with `MessagePackSerializer.Serialize(snapshot, ContractlessStandardResolver.Options)`, deserialize back, assert deep equality using FluentAssertions `.Should().BeEquivalentTo()`.

6. `SolutionSnapshot_DAG_Structure`: Create 3 projects (A -> B -> C) with 2 ProjectEdges. Assert the dependency structure is preserved after construction.

Use existing test patterns: xUnit [Fact], FluentAssertions, MessagePack with ContractlessStandardResolver.Options. Import `DocAgent.Core` namespace.
  </action>
  <verify>dotnet test --filter "FullyQualifiedName~SolutionSnapshot" -q && dotnet test -q</verify>
  <done>6 new tests pass. All existing 220+ tests still pass. SolutionSnapshot round-trips through MessagePack.</done>
</task>

</tasks>

<verification>
dotnet test -q
All tests pass (existing 220+ unchanged + 6 new solution type tests).
</verification>

<success_criteria>
- SolutionSnapshot, ProjectEntry, ProjectEdge records exist in DocAgent.Core
- SolutionSnapshot holds per-project SymbolGraphSnapshots (not a merged graph)
- ProjectEdge models the project dependency DAG
- MessagePack roundtrip test passes for SolutionSnapshot
- All existing tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/13-core-domain-extensions/13-02-SUMMARY.md`
</output>
