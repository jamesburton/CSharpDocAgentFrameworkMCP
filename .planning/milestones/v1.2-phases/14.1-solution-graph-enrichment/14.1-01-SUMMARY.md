---
phase: 14.1-solution-graph-enrichment
plan: 01
subsystem: ingestion
tags: [roslyn, msbuild, symbol-graph, solution-snapshot, edge-classification, stub-nodes]

requires:
  - phase: 13-core-domain-extensions
    provides: SolutionSnapshot, ProjectEdge, EdgeScope, NodeKind domain types
  - phase: 14-solution-ingestion-pipeline
    provides: SolutionIngestionService with PipelineOverride seam, IngestAsync pipeline

provides:
  - SolutionIngestionService.IngestAsync populates SolutionSnapshot with ProjectEntry DAG and per-project SymbolGraphSnapshots
  - EdgeScope classified at construction time (IntraProject/CrossProject/External) based on assembly membership
  - NodeKind.Stub synthesis for external type references (excluding common primitives)
  - Circular project reference detection via DFS; warns but does not crash
  - SolutionIngestionResult.Snapshot (nullable, backward-compatible) carries full SolutionSnapshot

affects: [phase-15-query-layer, phase-17-incremental-ingestion, any MCP tool using SolutionSnapshot]

tech-stack:
  added: []
  patterns:
    - "ProjectWalkContext readonly record struct — carries per-project walk state (name, solutionProjectNames, stubNodes, seenStubIds) passed by ref through namespace/type walkers"
    - "Edge scope set at construction time — ClassifyScope called inside WalkTypeInline when creating Inherits/Implements edges, not via post-classification pass"
    - "Stub deduplication via SeenStubIds HashSet — cross-project shared set prevents duplicate NodeKind.Stub entries for the same external type"
    - "DFS cycle detector on ProjectEdge list — simple path-tracking DFS, warnings added to result without removing edges"
    - "PipelineOverride seam tests verify OUTPUT contract — tests inject pre-built SolutionSnapshot via override, asserting result carries data correctly"

key-files:
  created: []
  modified:
    - src/DocAgent.McpServer/Ingestion/SolutionIngestionService.cs
    - src/DocAgent.McpServer/Ingestion/SolutionIngestionResult.cs
    - tests/DocAgent.Tests/SolutionIngestionServiceTests.cs

key-decisions:
  - "Edge scope classified at WalkTypeInline call site, not via post-classification pass — avoids a second O(N) pass and ensures edges are correct at origin"
  - "Shared seenStubIds HashSet across all projects — prevents duplicate stub nodes for types referenced by multiple projects"
  - "30-entry primitive filter (s_primitiveTypeNames) excludes System.String, Task, IEnumerable<T>, etc. — prevents index bloat from ubiquitous framework types"
  - "ProjectWalkContext as readonly record struct — value semantics with in parameter passing avoids heap allocation per node walk"
  - "DFS cycle detection includes both edges when cycle found — warns via ILogger and result.Warnings but does not remove edges, preserving graph fidelity"
  - "SolutionIngestionResult.Snapshot defaults to null — fully backward-compatible with existing callers that use positional record construction"

patterns-established:
  - "ProjectWalkContext: pass-by-ref context struct pattern for namespace/type walkers"
  - "ClassifyScope: pure helper that classifies edge scope without side effects"
  - "MaybeAddStubNode: guard-and-add pattern with primitive filter and deduplication"

requirements-completed: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04]

duration: 18min
completed: 2026-03-01
---

# Phase 14.1 Plan 01: SolutionSnapshot Enrichment Summary

**SolutionIngestionService now populates a full SolutionSnapshot with project DAG, cross/intra-project EdgeScope classification at construction time, and NodeKind.Stub synthesis for external NuGet/framework type references**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-03-01T18:00:02Z
- **Completed:** 2026-03-01T18:18:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added `ProjectWalkContext` struct and three enrichment capabilities to `SolutionIngestionService`: project DAG building, `EdgeScope` classification, and stub node synthesis
- Circular project reference detection via DFS — warns in logger and result Warnings list; edges are kept intact
- Added 7 new `PipelineOverride`-based tests; all 273 tests pass (266 pre-existing + 7 new)

## Task Commits

1. **Task 1: SolutionIngestionService enrichment** - `dcb3842` (feat)
2. **Task 2: 7 SolutionSnapshot enrichment tests** - `1f6b7d8` (test)

**Plan metadata:** (pending — created in final commit)

## Files Created/Modified

- `src/DocAgent.McpServer/Ingestion/SolutionIngestionService.cs` — Added `ProjectWalkContext`, `ClassifyScope`, `MaybeAddStubNode`, `DetectCycles`, and updated `WalkTypeInline`/`WalkNamespaceInline` signatures; IngestAsync now builds `SolutionSnapshot`
- `src/DocAgent.McpServer/Ingestion/SolutionIngestionResult.cs` — Added `SolutionSnapshot? Snapshot = null` optional parameter (backward-compatible)
- `tests/DocAgent.Tests/SolutionIngestionServiceTests.cs` — Added 7 new enrichment tests using PipelineOverride seam

## Decisions Made

- Edge scope classified at construction time inside `WalkTypeInline`, not via a post-classification pass — this was a locked decision from Phase 14 research
- `SolutionIngestionResult.Snapshot` defaults to `null` at the end of the positional record — backward-compatible with all existing callers
- Shared `seenStubIds` `HashSet<string>` across all projects prevents duplicate stub nodes for types referenced by multiple projects
- Primitive filter with 30 common framework types prevents index bloat

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `SolutionSnapshot` is now fully populated by `IngestAsync` — Phase 14.2+ plans can access the project DAG and per-project snapshots via `result.Snapshot`
- Edge scope is set at construction time — downstream query tools can filter by `EdgeScope.CrossProject` for cross-project analysis without additional processing
- Stub nodes use `NodeKind.Stub` and `ProjectOrigin = assemblyName` — Phase 15 search index can distinguish real from external symbols

---
*Phase: 14.1-solution-graph-enrichment*
*Completed: 2026-03-01*
