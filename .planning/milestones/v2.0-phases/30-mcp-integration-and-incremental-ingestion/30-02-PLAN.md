---
phase: 30-mcp-integration-and-incremental-ingestion
plan: 30-02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - src/DocAgent.Indexing/CamelCaseAnalyzer.cs
  - tests/DocAgent.Tests/CamelCaseAnalyzerTests.cs
  - tests/DocAgent.Tests/E2EIntegrationTests.cs
  - tests/DocAgent.Tests/TypeScriptToolVerificationTests.cs
autonomous: true
requirements:
  - MCPI-02
  - MCPI-04

must_haves:
  truths:
    - "BM25 search correctly matches camelCase TypeScript symbols"
    - "E2E test successfully ingests and queries a TypeScript project"
    - "All 14 MCP tools are verified to work with TypeScript snapshots"
  artifacts:
    - path: "src/DocAgent.Indexing/CamelCaseAnalyzer.cs"
      provides: "Improved camelCase tokenization"
    - path: "tests/DocAgent.Tests/CamelCaseAnalyzerTests.cs"
      provides: "CamelCase tokenization unit tests"
    - path: "tests/DocAgent.Tests/TypeScriptToolVerificationTests.cs"
      provides: "Cross-tool verification for all 14 MCP tools against TypeScript snapshots"
  key_links:
    - from: "tests/DocAgent.Tests/TypeScriptToolVerificationTests.cs"
      to: "src/DocAgent.McpServer/Tools/DocTools.cs"
      via: "Invokes all 7 query tools against TypeScript snapshot"
      pattern: "search_symbols|get_symbol|get_references|find_implementations|get_doc_coverage|diff_snapshots|explain_project"
    - from: "tests/DocAgent.Tests/TypeScriptToolVerificationTests.cs"
      to: "src/DocAgent.McpServer/Tools/ChangeTools.cs"
      via: "Invokes all 3 change-intelligence tools against TypeScript snapshots"
      pattern: "review_changes|find_breaking_changes|explain_change"
    - from: "tests/DocAgent.Tests/TypeScriptToolVerificationTests.cs"
      to: "src/DocAgent.McpServer/Tools/IngestionTools.cs"
      via: "Verifies C# ingestion tools still work alongside TypeScript snapshots"
      pattern: "ingest_project|ingest_solution"
    - from: "tests/DocAgent.Tests/TypeScriptToolVerificationTests.cs"
      to: "src/DocAgent.McpServer/Tools/SolutionTools.cs"
      via: "Verifies solution tools with mixed-language snapshot store"
      pattern: "explain_solution|diff_solution_snapshots"
---

<objective>
Optimize BM25 search for TypeScript's camelCase convention and verify the full system with E2E tests.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-mcp-integration-and-incremental-ingestion/30-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine CamelCaseAnalyzer</name>
  <files>src/DocAgent.Indexing/CamelCaseAnalyzer.cs, tests/DocAgent.Tests/CamelCaseAnalyzerTests.cs</files>
  <action>
    1. Update the `SplitRegex` in `CamelCaseTokenizer` to better handle lowercase-to-uppercase transitions (standard camelCase).
    2. Ensure that "getReferences" results in tokens "get" and "references" (via LowerCaseFilter).
    3. Add unit tests in `CamelCaseAnalyzerTests.cs` to verify:
       - "myMethodName" splits into "my", "method", "name"
       - "getReferences" splits into "get", "references"
       - "XMLParser" splits into "xml", "parser" (acronym handling)
       - "createHTTPServer" splits into "create", "http", "server"
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~CamelCaseAnalyzer"</automated>
  </verify>
  <done>
    Search tokenization is optimized for TypeScript camelCase conventions with passing unit tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: E2E Integration Tests</name>
  <files>tests/DocAgent.Tests/E2EIntegrationTests.cs</files>
  <action>
    Add a new test class `TypeScriptE2ETests.cs` or update `E2EIntegrationTests.cs`:
    1. Create a sample TypeScript project on disk (or use the one in `src/ts-symbol-extractor/tests/fixtures`).
    2. Call the `ingest_typescript` tool via an `McpClient` or directly via `IngestionTools`.
    3. Verify the resulting `snapshotId` is valid.
    4. Execute `search_symbols` and `get_symbol` on the TypeScript snapshot and verify results.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~E2EIntegration"</automated>
  </verify>
  <done>
    End-to-end flow is verified for TypeScript.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full Tool Verification — All 14 MCP Tools</name>
  <files>tests/DocAgent.Tests/TypeScriptToolVerificationTests.cs</files>
  <action>
    Create a verification suite that exercises ALL 14 MCP tools against TypeScript snapshots. The test fixture should ingest a TypeScript project to produce a snapshot, then ingest it again with a modified source file to produce a second snapshot for diff/change tools.

    **Query tools (7 tools) — verify against TypeScript snapshot:**
    1. `search_symbols` — search for a known TypeScript symbol, verify it appears in results with correct SymbolKind.
    2. `get_symbol` — retrieve a specific TypeScript symbol by SymbolId, verify doc comment and children.
    3. `get_references` — find references to a TypeScript interface, verify referencing symbols returned.
    4. `find_implementations` — find types implementing a TypeScript interface, verify implementors found.
    5. `get_doc_coverage` — verify TypeScript project appears with JSDoc coverage metrics.
    6. `diff_snapshots` — diff the two TypeScript snapshots, verify added/modified symbols detected.
    7. `explain_project` — verify TypeScript project overview includes namespaces, types, and stats.

    **Change intelligence tools (3 tools) — verify with two TypeScript snapshots:**
    8. `review_changes` — review changes between the two TypeScript snapshots, verify change groups returned.
    9. `find_breaking_changes` — verify breaking change detection between two TypeScript snapshots (e.g., removing a public export).
    10. `explain_change` — pick a modified symbol and verify the human-readable explanation includes before/after.

    **C# ingestion tools (2 tools) — regression verification with TypeScript snapshot in store:**
    11. `ingest_project` — ingest a C# .csproj while TypeScript snapshots exist in the store, verify it succeeds and returns a valid snapshot.
    12. `ingest_solution` — ingest a C# .sln while TypeScript snapshots exist, verify it succeeds without interference.

    **Solution tools (2 tools) — verify with mixed-language snapshot store:**
    13. `explain_solution` — after ingesting a C# solution alongside TypeScript snapshots, verify the solution overview returns valid structure.
    14. `diff_solution_snapshots` — ingest the same C# solution twice (with a minor change), diff the two solution snapshots, verify the diff structure is correct.

    For tools 11-14, the goal is regression verification: confirm C# tools continue to work correctly when TypeScript snapshots coexist in the SnapshotStore. These do not need deep assertion on TypeScript content — just verify no errors and valid response shapes.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~TypeScriptToolVerification"</automated>
  </verify>
  <done>
    All 14 MCP tools are verified: 7 query tools and 3 change-intelligence tools produce correct results with TypeScript snapshots; 4 C#-focused tools (ingest_project, ingest_solution, explain_solution, diff_solution_snapshots) continue to work correctly with TypeScript snapshots in the store.
  </done>
</task>

</tasks>

<verification>
- All unit and integration tests pass.
- All 14 MCP tools exercised in TypeScriptToolVerificationTests.
- Manual verification of search quality for TypeScript symbols.
</verification>

<success_criteria>
- Search quality parity between C# and TypeScript.
- Verified E2E ingestion and query flow.
- All 14 MCP tools confirmed working with TypeScript snapshots (MCPI-02).
</success_criteria>

<output>
After completion, create `.planning/phases/30-mcp-integration-and-incremental-ingestion/30-02-SUMMARY.md`
</output>
