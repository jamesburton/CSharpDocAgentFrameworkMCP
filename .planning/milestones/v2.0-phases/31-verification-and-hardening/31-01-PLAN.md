---
phase: 31-verification-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/DocAgent.Tests/TypeScriptStressTests.cs
  - tests/DocAgent.Benchmarks/TypeScriptIngestionBenchmarks.cs
  - tests/DocAgent.Tests/TypeScriptDeterminismTests.cs
autonomous: true
requirements: [VERF-01, VERF-04]
must_haves:
  truths:
    - "Ingesting a 100+ file TypeScript project completes without errors"
    - "Warm start (incremental hit) is significantly faster than cold start"
    - "Two identical ingestions of the same TS project produce identical snapshot hashes"
    - "Searching symbols in a large TypeScript graph returns correct results with low latency"
  artifacts:
    - path: "tests/DocAgent.Tests/TypeScriptStressTests.cs"
      provides: "Stress test for large TS project ingestion"
    - path: "tests/DocAgent.Benchmarks/TypeScriptIngestionBenchmarks.cs"
      provides: "Benchmarking for cold/warm TS ingestion performance"
    - path: "tests/DocAgent.Tests/TypeScriptDeterminismTests.cs"
      provides: "C#-side determinism validation for TS snapshots"
  key_links:
    - from: "tests/DocAgent.Tests/TypeScriptStressTests.cs"
      to: "src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs"
      via: "IngestTypeScriptAsync call"
---

<objective>
Verify the performance and stability of the TypeScript ingestion pipeline against larger-scale projects.

Purpose: Ensure the sidecar-based approach scales well and maintains determinism for v2.0.
Output: Stress tests, performance benchmarks, and determinism verification.
</objective>

<execution_context>
@C:/Users/james/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-mcp-integration-and-incremental-ingestion/30-02-SUMMARY.md
@src/DocAgent.McpServer/Ingestion/TypeScriptIngestionService.cs
@src/ts-symbol-extractor/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Large-Scale TypeScript Stress Test</name>
  <files>tests/DocAgent.Tests/TypeScriptStressTests.cs</files>
  <action>
    Create a new test class `TypeScriptStressTests.cs` that includes a utility to generate a synthetic TypeScript project with 100+ source files.
    The generated project should include:
    - Deeply nested namespaces (modules).
    - Interfaces with multiple implementations across different files.
    - Large classes with numerous methods and JSDoc blocks.
    - Cross-file imports and exports.
    Verify that `TypeScriptIngestionService.IngestTypeScriptAsync` can successfully ingest the entire project and that the resulting `SymbolGraphSnapshot` contains all expected nodes and edges without data loss.
  </action>
  <verify>
    Run `dotnet test --filter TypeScriptStressTests` and confirm it passes for a 100-file project.
  </verify>
  <done>
    Large-scale synthetic TS project successfully ingested into a valid SymbolGraphSnapshot.
  </done>
</task>

<task type="auto">
  <name>Task 2: Performance Profiling and Memory Benchmarks</name>
  <files>tests/DocAgent.Benchmarks/TypeScriptIngestionBenchmarks.cs</files>
  <action>
    Create a new benchmark file `TypeScriptIngestionBenchmarks.cs` in the `DocAgent.Benchmarks` project.
    Implement benchmarks to measure:
    - Cold Start: Ingesting a large TS project with no existing file manifest.
    - Warm Start (Incremental Hit): Re-ingesting the same project with no changes (verifying manifest-based skipping).
    - Sidecar Memory Usage: Use `System.Diagnostics.Process` to track the peak `PrivateMemorySize64` of the sidecar process during execution.
    Log the results and ensure they stay within reasonable thresholds (e.g., warm start < 1s for 100 files).
  </action>
  <verify>
    Run the benchmarks via `dotnet run -c Release --project tests/DocAgent.Benchmarks` and review the output.
  </verify>
  <done>
    Performance metrics recorded and verified for cold/warm TS ingestion.
  </done>
</task>

<task type="auto">
  <name>Task 3: Snapshot Determinism and MCP Tool Round-Trip</name>
  <files>tests/DocAgent.Tests/TypeScriptDeterminismTests.cs</files>
  <action>
    Create `TypeScriptDeterminismTests.cs` to verify that repeated ingestions of the same TypeScript source produce byte-identical `SymbolGraphSnapshot` hashes.
    Then, extend the test to run all 14 existing MCP tools (starting with `search_symbols`, `get_symbol`, and `get_references`) against the large-scale TS snapshot produced in Task 1.
    Verify that the tools return correct results and that search latency remains low (< 50ms) even with a larger graph (1000+ nodes).
  </action>
  <verify>
    Run `dotnet test --filter TypeScriptDeterminismTests` and confirm all cross-tool validation passes.
  </verify>
  <done>
    Snapshot determinism confirmed and all MCP tools verified against large-scale TS data.
  </done>
</task>

</tasks>

<verification>
1. Stress test successfully ingests 100+ files.
2. Benchmarks confirm incremental ingestion speed gains.
3. Determinism test confirms identical hashes for identical inputs.
4. Search performance verified on a large TypeScript graph.
</verification>

<success_criteria>
1. `TypeScriptStressTests` passes for 100+ files.
2. `TypeScriptIngestionBenchmarks` shows warm-start latency < 1.5s for 100 files.
3. `TypeScriptDeterminismTests` confirms 100% hash stability across multiple runs.
</success_criteria>

<output>
After completion, create `.planning/phases/31-verification-and-hardening/31-01-SUMMARY.md`
</output>
