---
phase: 31-verification-and-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ts-symbol-extractor/src/extractor.ts
  - src/ts-symbol-extractor/src/index.ts
  - tests/DocAgent.Tests/TypeScriptRobustnessTests.cs
autonomous: true
requirements: [VERF-03]
gap_closure: true
must_haves:
  truths:
    - "No absolute paths are leaked in SymbolNode.Span.FilePath fields"
    - "All debug console.error statements are removed from the production sidecar"
  artifacts:
    - path: "src/ts-symbol-extractor/src/extractor.ts"
      provides: "getSourceSpan returns relative file paths instead of absolute OS paths"
      contains: "path.relative"
    - path: "src/ts-symbol-extractor/src/index.ts"
      provides: "Clean sidecar with only fatal error logging"
    - path: "tests/DocAgent.Tests/TypeScriptRobustnessTests.cs"
      provides: "Test asserting no absolute paths in Span.FilePath"
  key_links:
    - from: "src/ts-symbol-extractor/src/extractor.ts"
      to: "SymbolNode.Span.FilePath"
      via: "getSourceSpan returns path.relative(projectRoot, sourceFile.fileName)"
      pattern: "path\\.relative"
---

<objective>
Fix the two most critical sidecar defects found in Phase 31 verification: absolute path leaks in SymbolNode.Span.FilePath and debug console.error statements in production code.

Purpose: Gap 1 is a security blocker — every TypeScript snapshot leaks the user's absolute machine path (e.g. `C:/Users/james/...`) into every SymbolNode.Span.FilePath. Gap 2 is an information disclosure issue — debug logging emits request content on every invocation.
Output: Patched extractor.ts, cleaned index.ts, and a new robustness test proving no absolute paths leak.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-verification-and-hardening/31-VERIFICATION.md

<interfaces>
<!-- Key function and call sites in extractor.ts that need modification -->

From src/ts-symbol-extractor/src/extractor.ts (line 279):
```typescript
function getSourceSpan(node: ts.Node, sourceFile: ts.SourceFile): SourceSpan {
  // Currently uses sourceFile.fileName (absolute) — MUST change to relative
  return {
    filePath: sourceFile.fileName.replace(/\\/g, '/'),
    // ...
  };
}
```

Call sites for getSourceSpan (lines 66, 130):
- Line 66: `span: getSourceSpan(sourceFile, sourceFile)` — inside extractSymbols where projectRoot is in scope
- Line 130: `span: getSourceSpan(node, sourceFile)` — inside extractDeclaration where projectRoot is parameter

From src/ts-symbol-extractor/src/extractor.ts (line 56):
```typescript
const relativePath = path.relative(projectRoot, normalizedOriginalFileName).replace(/\\/g, '/');
```
The relativePath computation already exists — the fix is to pass projectRoot into getSourceSpan and use the same logic there.

From src/ts-symbol-extractor/src/index.ts (lines 10, 71):
```typescript
// Line 10 (inside handleRequest): console.error(`Sidecar received: ...`)
// Line 71 (inside main loop): console.error(`Sidecar processing line ...`)
// Line 97 (fatal — KEEP): console.error('Fatal error in sidecar:', err)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix absolute path leak in getSourceSpan and remove debug logging</name>
  <files>src/ts-symbol-extractor/src/extractor.ts, src/ts-symbol-extractor/src/index.ts</files>
  <action>
**extractor.ts — Fix getSourceSpan (line 279):**
1. Add `projectRoot: string` as a third parameter to `getSourceSpan()`:
   `function getSourceSpan(node: ts.Node, sourceFile: ts.SourceFile, projectRoot: string): SourceSpan`
2. Replace the `filePath` assignment (line 284) with:
   `filePath: path.relative(projectRoot, sourceFile.fileName).replace(/\\/g, '/')`
3. Ensure `import path from 'node:path'` is at the top (it should already be there — verify).
4. Update both call sites:
   - Line 66: `span: getSourceSpan(sourceFile, sourceFile, projectRoot)` — projectRoot is already in scope from `extractSymbols` function parameter
   - Line 130: `span: getSourceSpan(node, sourceFile, projectRoot)` — projectRoot is already in scope from `extractDeclaration` function parameter

**index.ts — Remove debug console.error statements:**
1. Delete line 10: `console.error(\`Sidecar received: ${line.substring(0, 100)}...\`);` (inside handleRequest)
2. Delete line 71: `console.error(\`Sidecar processing line (length ${line.length}): ...\`);` (inside main for-await loop)
3. Keep the fatal error handler on line 97: `console.error('Fatal error in sidecar:', err)` — this is a crash handler and must stay.

**After edits, rebuild the sidecar bundle:**
Run `cd src/ts-symbol-extractor && npm run build` to regenerate dist/index.js.
  </action>
  <verify>
    <automated>cd src/ts-symbol-extractor && npm run build && npm test</automated>
  </verify>
  <done>getSourceSpan produces relative paths (e.g. "src/utils.ts" not "C:/Users/.../src/utils.ts"). Only the fatal crash handler console.error remains in index.ts. Sidecar builds and existing vitest tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add robustness test asserting no absolute paths in Span.FilePath</name>
  <files>tests/DocAgent.Tests/TypeScriptRobustnessTests.cs</files>
  <behavior>
    - Test: IngestTypeScriptAsync_produces_relative_file_paths_in_spans — ingest a TypeScript project using PipelineOverride, then assert every SymbolNode.Span.FilePath does NOT start with '/' and does NOT match regex `^[A-Za-z]:[\\/]` (Windows drive letter pattern). All paths should be relative (e.g., "src/index.ts").
  </behavior>
  <action>
Add a new test method `IngestTypeScriptAsync_produces_relative_file_paths_in_spans` to the existing `TypeScriptRobustnessTests` class.

The test should:
1. Create a temp directory with a minimal tsconfig.json and one .ts file (e.g., `src/hello.ts` with `export function hello(): string { return 'hi'; }`)
2. Call `IngestTypeScriptAsync` (using the real sidecar pipeline — NOT PipelineOverride, because we need the actual extractor to run and produce spans)
3. Retrieve the snapshot from the SnapshotStore
4. Iterate ALL `SymbolNode` entries that have a non-null `Span`
5. Assert: `node.Span.FilePath.Should().NotStartWith("/")` — not Unix absolute
6. Assert: `node.Span.FilePath.Should().NotMatchRegex(@"^[A-Za-z]:[/\\\\]")` — not Windows absolute
7. Assert: At least one span exists (sanity check the test is meaningful)

Follow the existing pattern in TypeScriptRobustnessTests for service resolution and test setup (use `_tsService` field and the existing DI setup). If the existing tests use PipelineOverride for speed, this specific test MUST use the real sidecar because the path computation happens in the TypeScript extractor — PipelineOverride would bypass it.

Note: This test requires Node.js to be installed. If other tests in the class already call the real sidecar (e.g., `handles_ts_syntax_errors_gracefully`), follow that same pattern.
  </action>
  <verify>
    <automated>dotnet test --filter "FullyQualifiedName~TypeScriptRobustnessTests.IngestTypeScriptAsync_produces_relative_file_paths_in_spans" --no-build</automated>
  </verify>
  <done>Test passes green, proving all Span.FilePath values in a real TypeScript ingestion are relative paths with no absolute OS path prefix.</done>
</task>

</tasks>

<verification>
1. `cd src/ts-symbol-extractor && npm test` — all vitest tests pass
2. `dotnet test --filter "FullyQualifiedName~TypeScriptRobustnessTests"` — all robustness tests pass including new path test
3. `dotnet test` — full suite passes with no regressions
4. Manual grep: `grep -n "console.error" src/ts-symbol-extractor/src/index.ts` shows only the fatal error handler
5. Manual grep: `grep -n "sourceFile.fileName" src/ts-symbol-extractor/src/extractor.ts` shows no direct use in filePath assignment
</verification>

<success_criteria>
- Every SymbolNode.Span.FilePath in TypeScript snapshots contains a relative path (no absolute OS paths)
- Only one console.error statement remains in index.ts (the fatal crash handler)
- Sidecar vitest tests pass
- Full dotnet test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/31-verification-and-hardening/31-03-SUMMARY.md`
</output>
