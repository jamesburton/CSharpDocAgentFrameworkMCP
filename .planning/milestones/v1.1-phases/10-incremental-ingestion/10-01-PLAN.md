---
phase: 10-incremental-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DocAgent.Core/IngestionMetadata.cs
  - src/DocAgent.Core/Symbols.cs
  - src/DocAgent.Ingestion/FileHashManifest.cs
  - tests/DocAgent.Tests/IncrementalIngestion/FileHashManifestTests.cs
  - tests/DocAgent.Tests/IncrementalIngestion/IngestionMetadataTests.cs
autonomous: true
requirements:
  - R-INCR-INGEST

must_haves:
  truths:
    - "IngestionMetadata type captures run ID, timestamps, full-vs-incremental flag, and file change records"
    - "SymbolGraphSnapshot has optional IngestionMetadata field (null-safe for old snapshots)"
    - "FileHashManifest can compute SHA-256 hashes, diff two manifests, and round-trip to JSON"
  artifacts:
    - path: "src/DocAgent.Core/IngestionMetadata.cs"
      provides: "IngestionMetadata, FileChangeRecord, FileChangeKind types"
      exports: ["IngestionMetadata", "FileChangeRecord", "FileChangeKind"]
    - path: "src/DocAgent.Core/Symbols.cs"
      provides: "SymbolGraphSnapshot with optional IngestionMetadata field"
      contains: "IngestionMetadata?"
    - path: "src/DocAgent.Ingestion/FileHashManifest.cs"
      provides: "FileHashManifest record, FileHasher, ManifestDiff"
      exports: ["FileHashManifest", "FileHasher", "ManifestDiff"]
  key_links:
    - from: "src/DocAgent.Core/Symbols.cs"
      to: "src/DocAgent.Core/IngestionMetadata.cs"
      via: "SymbolGraphSnapshot.IngestionMetadata property"
      pattern: "IngestionMetadata\\?"
---

<objective>
Define the core domain types for incremental ingestion and the file hash manifest for change detection.

Purpose: Establish the type contracts that Plan 02 (IncrementalIngestionEngine) builds against. IngestionMetadata travels with the snapshot; FileHashManifest persists between runs for change detection.
Output: IngestionMetadata.cs, updated Symbols.cs, FileHashManifest.cs, unit tests for both.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-incremental-ingestion/10-CONTEXT.md
@.planning/phases/10-incremental-ingestion/10-RESEARCH.md

@src/DocAgent.Core/Symbols.cs
@src/DocAgent.Ingestion/SnapshotStore.cs

<interfaces>
<!-- Existing types the executor needs -->

From src/DocAgent.Core/Symbols.cs:
```csharp
public sealed record SymbolGraphSnapshot(
    string SchemaVersion,
    string ProjectName,
    string SourceFingerprint,
    string? ContentHash,
    DateTimeOffset CreatedAt,
    IReadOnlyList<SymbolNode> Nodes,
    IReadOnlyList<SymbolEdge> Edges);
```

From src/DocAgent.Ingestion/SnapshotStore.cs:
```csharp
// JSON serialization pattern used for manifest
private static readonly JsonSerializerOptions JsonOptions = new()
{
    WriteIndented = true,
    PropertyNameCaseInsensitive = true
};
// Atomic write pattern: write temp, File.Move with overwrite
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IngestionMetadata domain types and update SymbolGraphSnapshot</name>
  <files>src/DocAgent.Core/IngestionMetadata.cs, src/DocAgent.Core/Symbols.cs, tests/DocAgent.Tests/IncrementalIngestion/IngestionMetadataTests.cs</files>
  <action>
1. Create `src/DocAgent.Core/IngestionMetadata.cs` with:
   - `public enum FileChangeKind { Added, Modified, Removed }`
   - `public sealed record FileChangeRecord(string FilePath, FileChangeKind ChangeKind, IReadOnlyList<string> AffectedSymbolIds)`
   - `public sealed record IngestionMetadata(string RunId, DateTimeOffset StartedAt, DateTimeOffset CompletedAt, bool WasFullReingestion, IReadOnlyList<FileChangeRecord> FileChanges)`

2. Update `src/DocAgent.Core/Symbols.cs` — add `IngestionMetadata? IngestionMetadata = null` as the LAST parameter of the `SymbolGraphSnapshot` record (MUST be last for MessagePack ContractlessStandardResolver backward compatibility).

3. Create `tests/DocAgent.Tests/IncrementalIngestion/IngestionMetadataTests.cs`:
   - Test that IngestionMetadata round-trips through MessagePack serialization (ContractlessStandardResolver)
   - Test that a SymbolGraphSnapshot with IngestionMetadata=null deserializes correctly (backward compat)
   - Test that a SymbolGraphSnapshot with IngestionMetadata set round-trips correctly
   - Follow existing `SnapshotSerializationTests` pattern for MessagePack round-trip tests

IMPORTANT: After updating SymbolGraphSnapshot, check that ALL existing `new SymbolGraphSnapshot(...)` call sites still compile (the new param has a default so they should, but verify with build).
  </action>
  <verify>dotnet build src/DocAgentFramework.sln && dotnet test --filter "FullyQualifiedName~IngestionMetadataTests"</verify>
  <done>IngestionMetadata types exist in Core, SymbolGraphSnapshot has optional IngestionMetadata field, serialization round-trip tests pass, existing code compiles unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Create FileHashManifest with SHA-256 hashing, diff, and JSON persistence</name>
  <files>src/DocAgent.Ingestion/FileHashManifest.cs, tests/DocAgent.Tests/IncrementalIngestion/FileHashManifestTests.cs</files>
  <action>
1. Create `src/DocAgent.Ingestion/FileHashManifest.cs` with:
   - `public sealed record FileHashManifest(IReadOnlyDictionary<string, string> FileHashes, DateTimeOffset CreatedAt, string SchemaVersion = "1.0")`
     - `FileHashes`: relative or absolute file path -> SHA-256 hex string (lowercase)
   - `public static class FileHasher`:
     - `public static async Task<string> ComputeAsync(string filePath, CancellationToken ct)` — streaming SHA-256 via `SHA256.HashDataAsync(stream, ct)`, returns lowercase hex
     - `public static async Task<FileHashManifest> ComputeManifestAsync(IReadOnlyList<string> filePaths, CancellationToken ct)` — hash all files, return manifest
   - `public sealed record ManifestDiff(IReadOnlyList<string> AddedFiles, IReadOnlyList<string> ModifiedFiles, IReadOnlyList<string> RemovedFiles)` with `bool HasChanges` and `IReadOnlyList<string> ChangedFiles` (Added + Modified)
   - `public static ManifestDiff Diff(FileHashManifest? previous, FileHashManifest current)` — null previous = all files added
   - `public static async Task SaveAsync(FileHashManifest manifest, string path, CancellationToken ct)` — JSON, atomic write (temp + rename, matching SnapshotStore pattern)
   - `public static async Task<FileHashManifest?> LoadAsync(string path, CancellationToken ct)` — returns null if file doesn't exist

2. Create `tests/DocAgent.Tests/IncrementalIngestion/FileHashManifestTests.cs`:
   - Test SHA-256 hash of a known file content produces expected hex
   - Test Diff with null previous (all added)
   - Test Diff with added/modified/removed files
   - Test JSON round-trip (save then load)
   - Test HasChanges/ChangedFiles convenience properties
  </action>
  <verify>dotnet test --filter "FullyQualifiedName~FileHashManifestTests"</verify>
  <done>FileHashManifest can hash files, diff manifests, persist to JSON, and all unit tests pass</done>
</task>

</tasks>

<verification>
dotnet build src/DocAgentFramework.sln
dotnet test --filter "FullyQualifiedName~IncrementalIngestion"
</verification>

<success_criteria>
- IngestionMetadata, FileChangeRecord, FileChangeKind types exist in DocAgent.Core
- SymbolGraphSnapshot record has optional IngestionMetadata parameter (last position)
- FileHashManifest with SHA-256 hashing, diff computation, and JSON persistence exists
- All existing tests still pass (no regressions from SymbolGraphSnapshot change)
- New unit tests pass for both IngestionMetadata serialization and FileHashManifest operations
</success_criteria>

<output>
After completion, create `.planning/phases/10-incremental-ingestion/10-01-SUMMARY.md`
</output>
