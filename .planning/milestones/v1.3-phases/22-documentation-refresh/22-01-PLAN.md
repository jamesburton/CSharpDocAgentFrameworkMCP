---
phase: 22-documentation-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/Architecture.md
  - docs/Plan.md
  - docs/Testing.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
must_haves:
  truths:
    - "Architecture.md names all 6 projects (Core, Ingestion, Indexing, McpServer, AppHost, Analyzers)"
    - "Architecture.md lists all 12 MCP tools grouped by domain (DocTools, ChangeTools, SolutionTools, IngestionTools)"
    - "Architecture.md includes project dependency table and Mermaid diagrams"
    - "Plan.md reflects v1.0-v1.2 as shipped milestones with key features and MCP tools delivered"
    - "Plan.md has no phantom features (GitRepoSource, PackageRefGraph, embeddings impl, IndexWriter path)"
    - "Plan.md preserves deferred/speculative items in a Future section"
    - "Testing.md states current test count (330 total, 309 passing, 21 environment-dependent)"
    - "Testing.md describes test categories with counts, validators, and example classes"
    - "Testing.md has Known Limitations section for MSBuildWorkspace and RegressionGuard failures"
  artifacts:
    - path: "docs/Architecture.md"
      provides: "Complete architecture reference with 6 projects, 12 tools, pipeline, dependencies"
    - path: "docs/Plan.md"
      provides: "Shipped milestone history plus future plans"
    - path: "docs/Testing.md"
      provides: "Test strategy, category breakdown, commands, known limitations"
  key_links:
    - from: "docs/Architecture.md"
      to: "docs/Security.md"
      via: "brief security section with pointer"
    - from: "docs/Testing.md"
      to: "CLAUDE.md"
      via: "references for build/test commands"
---

<objective>
Refresh Architecture.md, Plan.md, and Testing.md to accurately reflect the v1.0-v1.2 shipped codebase and current test suite.

Purpose: The three docs were written as planning artifacts (2026-02-25) and have never been updated. They describe aspirations, not reality. Developers picking up the codebase get incorrect information.
Output: Three updated Markdown files matching shipped reality, with concise reference style, tables, and Mermaid diagrams.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-documentation-refresh/22-RESEARCH.md

Source files for fact verification:
@src/DocAgent.McpServer/Tools/DocTools.cs
@src/DocAgent.McpServer/Tools/ChangeTools.cs
@src/DocAgent.McpServer/Tools/SolutionTools.cs
@src/DocAgent.McpServer/Tools/IngestionTools.cs
@docs/Architecture.md
@docs/Plan.md
@docs/Testing.md
@docs/Security.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Architecture.md to reflect shipped system</name>
  <files>docs/Architecture.md</files>
  <action>
Rewrite docs/Architecture.md as a concise reference document. Use 22-RESEARCH.md ground truth data — do NOT copy from the old doc or from memory.

**Structure (follow CONTEXT.md locked decisions):**

1. **Header** — One-line purpose statement referencing CLAUDE.md for quick-start.

2. **Projects** — Table with all 6 projects: DocAgent.Core, DocAgent.Ingestion, DocAgent.Indexing, DocAgent.McpServer, DocAgent.AppHost, DocAgent.Analyzers. Columns: Project, Target Framework, Responsibility.

3. **Project Dependencies** — Table showing which projects reference which (from 22-RESEARCH.md "Project Dependency Graph"). Include a Mermaid `graph TD` diagram.

4. **Pipeline** — Show the full pipeline with interface names:
   `IProjectSource → IDocSource → ISymbolGraphBuilder → ISearchIndex → IKnowledgeQueryService → MCP Tools`
   Plus the incremental path (IncrementalIngestionEngine, IncrementalSolutionIngestionService).
   Include a Mermaid `flowchart LR` diagram.

5. **MCP Tools** — Group all 12 tools by domain per CONTEXT.md decision:
   - DocTools (5): search_symbols, get_symbol, get_references, diff_snapshots, explain_project
   - ChangeTools (3): review_changes, find_breaking_changes, explain_change
   - SolutionTools (2): explain_solution, diff_solution_snapshots
   - IngestionTools (2): ingest_project, ingest_solution
   Table columns: Tool Name, Description (one line each).

6. **Security** — Brief paragraph noting default-deny PathAllowlist, AuditLogger, PromptInjectionScanner. Pointer to Security.md for details.

7. **Storage** — Snapshots written to artifacts/ directory, MessagePack serialization, pointer files for incremental state.

**Style:** Concise reference — tables, diagrams, key facts. Match CLAUDE.md tone. No prose filler. Verify every tool name against `[McpServerTool(Name = "...")]` attributes in source, not method names.
  </action>
  <verify>
    <automated>grep -c "search_symbols\|get_symbol\|get_references\|diff_snapshots\|explain_project\|review_changes\|find_breaking_changes\|explain_change\|explain_solution\|diff_solution_snapshots\|ingest_project\|ingest_solution" docs/Architecture.md</automated>
    Expect count >= 12 (all tool names present). Also verify: grep "DocAgent.Analyzers" docs/Architecture.md returns a match.
  </verify>
  <done>Architecture.md names all 6 projects, lists all 12 MCP tools by domain, has project dependency table, Mermaid diagrams, pipeline with interface names, and brief security pointer.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Plan.md to reflect shipped milestones</name>
  <files>docs/Plan.md</files>
  <action>
Rewrite docs/Plan.md as a shipped milestone history plus future plans. Use 22-RESEARCH.md and .planning/PROJECT.md as ground truth.

**Structure (follow CONTEXT.md locked decisions):**

1. **Header** — Project name, one-line description.

2. **Shipped Milestones** — One section per shipped milestone:
   - **v1.0 MVP** (shipped 2026-02-28): Core domain, ingestion pipeline, BM25 search index, query facade, MCP server with security, Aspire hosting, ingestion triggers, Roslyn analyzers. Tools delivered: search_symbols, get_symbol, get_references, diff_snapshots, explain_project, ingest_project.
   - **v1.1 Semantic Diff & Change Intelligence** (shipped 2026-03-01): Semantic diff engine, incremental file-level ingestion, change reviewer. Tools delivered: review_changes, find_breaking_changes, explain_change.
   - **v1.2 Multi-Project & Solution-Level Graphs** (shipped 2026-03-02): Solution-level types, cross-project edges, stub nodes, project-aware search, FQN disambiguation. Tools delivered: explain_solution, diff_solution_snapshots, ingest_solution.
   - **v1.3 Housekeeping** (in progress / shipped 2026-03-03): Incremental solution re-ingestion, MSBuild performance benchmarks, code cleanup, documentation refresh.

   For each milestone, list key capabilities and MCP tools delivered in that milestone.

3. **Future Milestones** — Preserve speculative/deferred items:
   - v1.5: Package mapping (PKG-01/02)
   - Future: Polyglot support (Tree-sitter, LSP), embeddings/vector index, query DSL
   - V2/V3 aspirations (brief)

**CRITICAL — Remove phantom features:**
- GitRepoSource (never shipped)
- PackageRefGraph as V1 deliverable (deferred to v1.5)
- Embeddings implementation (interface only, no impl)
- IndexWriter writing to artifacts/index/ (not how it works)

**Style:** Concise tables and bullet points. Each milestone is a self-contained section.
  </action>
  <verify>
    <automated>grep -c "GitRepoSource\|PackageRefGraph" docs/Plan.md</automated>
    Expect 0 matches (phantom features removed) OR only in a "Future" context for PackageRefGraph. Also: grep "v1.1\|v1.2" docs/Plan.md should return matches.
  </verify>
  <done>Plan.md shows v1.0-v1.2 as shipped milestones with MCP tools per milestone, no phantom features listed as delivered, deferred items in Future section.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Testing.md with current test suite facts</name>
  <files>docs/Testing.md</files>
  <action>
Rewrite docs/Testing.md as a test strategy and how-to-run reference. Use 22-RESEARCH.md test suite facts as ground truth.

**Structure (follow CONTEXT.md locked decisions):**

1. **Header** — One-line description of test approach.

2. **Test Philosophy** — Brief: xUnit + FluentAssertions, TreatWarningsAsErrors, no implicit network calls, deterministic snapshots.

3. **Running Tests** — Commands:
   ```bash
   dotnet test                                          # All tests
   dotnet test --filter "FullyQualifiedName~XmlDocParserTests"  # Single class
   dotnet test --filter "FullyQualifiedName~IncrementalIngestion" # Category
   ```

4. **Test Categories** — Table with columns: Category, Approx Count, What It Validates, Example Test Class. Use the categories from 22-RESEARCH.md:
   - XML parsing + ingestion (~3 files)
   - Roslyn symbol graph (~2)
   - Search indexing (~3)
   - Snapshot serialization + store (~2)
   - Incremental ingestion (~7)
   - Semantic diff (~8)
   - Change review (~2)
   - MCP tools + integration (~8)
   - Security (~3)
   - Solution-level (~5)
   - Roslyn analyzers (~3)
   - Performance / regression (~1)
   - E2E + determinism (~4)

5. **Fixture Patterns** — Brief descriptions:
   - Golden files for snapshot determinism
   - PipelineOverride seam for MSBuild-free tests
   - In-proc MCP server for tool integration tests

6. **Known Limitations** — Document the 21 environment-dependent failures:
   - MSBuildWorkspace tests: require MSBuild toolchain at runtime, fail without pre-warmed dotnet build
   - RegressionGuardTests: BenchmarkDotNet ResultStatistics null outside proper release-mode benchmark run
   - These are infrastructure constraints, not code bugs

7. **Summary line** — "330 total tests | 309 passing | 21 environment-dependent"

**Style:** Tables and bullet points. Practical reference for a developer running tests.
  </action>
  <verify>
    <automated>grep -c "330\|309\|21" docs/Testing.md</automated>
    Expect count >= 3 (all three numbers present). Also: grep "Known Limitations" docs/Testing.md should return a match.
  </verify>
  <done>Testing.md states 330 total / 309 passing / 21 environment-dependent, has category table with example classes, run commands, fixture patterns, and Known Limitations section.</done>
</task>

</tasks>

<verification>
1. All 12 MCP tool wire names appear in Architecture.md
2. All 6 project names appear in Architecture.md
3. Plan.md mentions v1.0, v1.1, v1.2 as shipped
4. Plan.md does not list GitRepoSource as a shipped feature
5. Testing.md contains "330" total test count
6. Testing.md has "Known Limitations" section
7. `dotnet build src/DocAgentFramework.sln` still succeeds (no code changes, sanity check)
</verification>

<success_criteria>
- Architecture.md names all 6 projects and all 12 MCP tools correctly (DOCS-01)
- Plan.md reflects v1.0-v1.2 as shipped with no phantom features (DOCS-02)
- Testing.md states current test count and describes actual strategy (DOCS-03)
</success_criteria>

<output>
After completion, create `.planning/phases/22-documentation-refresh/22-01-SUMMARY.md`
</output>
