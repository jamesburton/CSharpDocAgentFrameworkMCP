---
phase: 21-code-and-audit-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DocAgent.Indexing/InMemorySearchIndex.cs
  - src/DocAgent.Indexing/KnowledgeQueryService.cs
  - .planning/milestones/v1.2-phases/16-solution-mcp-tools/16-01-SUMMARY.md
  - tests/DocAgent.Benchmarks/SolutionIngestionBenchmarks.cs
  - src/DocAgent.McpServer/Program.cs
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "InMemorySearchIndex.cs contains no 'TODO: replace with BM25' comment"
    - "KnowledgeQueryService.cs contains no stale 'stub' label at the GetReferencesAsync section header"
    - "16-01-SUMMARY.md frontmatter includes requirements_completed field"
    - "IncrementalNoChange benchmark exercises the incremental skip path via IncrementalSolutionIngestionService"
    - "Program.cs registers DocAgent.Ingestion meter for OTel counter collection"
  artifacts:
    - path: "src/DocAgent.Indexing/InMemorySearchIndex.cs"
      provides: "Clean search index without stale TODO"
    - path: "src/DocAgent.Indexing/KnowledgeQueryService.cs"
      provides: "Clean query service without stale stub label"
    - path: "tests/DocAgent.Benchmarks/SolutionIngestionBenchmarks.cs"
      provides: "Benchmark using IncrementalSolutionIngestionService"
    - path: "src/DocAgent.McpServer/Program.cs"
      provides: "OTel meter registration for DocAgent.Ingestion"
  key_links:
    - from: "tests/DocAgent.Benchmarks/SolutionIngestionBenchmarks.cs"
      to: "src/DocAgent.McpServer/Ingestion/IncrementalSolutionIngestionService.cs"
      via: "GlobalSetup wraps SolutionIngestionService in IncrementalSolutionIngestionService decorator"
    - from: "src/DocAgent.McpServer/Program.cs"
      to: "src/DocAgent.McpServer/Ingestion/IncrementalSolutionIngestionService.cs"
      via: "AddMeter name matches Meter constructor string"
---

<objective>
Remove stale code comments, fix audit artifact frontmatter, wire IncrementalNoChange benchmark to the decorator service, and register the OTel meter in Program.cs.

Purpose: Close QUAL-01/02/03 requirements and fix two integration wiring gaps identified in the v1.3 milestone audit.
Output: Clean codebase with all v1.3 audit issues resolved; dotnet test passes.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-code-and-audit-cleanup/21-RESEARCH.md

<interfaces>
<!-- From src/DocAgent.McpServer/Ingestion/IncrementalSolutionIngestionService.cs -->
public class IncrementalSolutionIngestionService : ISolutionIngestionService
{
    public IncrementalSolutionIngestionService(
        SnapshotStore store,
        SolutionIngestionService fullIngestionService,
        ILogger<IncrementalSolutionIngestionService> logger)
}

<!-- From tests/DocAgent.Benchmarks/SolutionIngestionBenchmarks.cs -->
// Field at line 20: private SolutionIngestionService _service;
// GlobalSetup at lines 35-38 constructs SolutionIngestionService directly
// IncrementalNoChange benchmark calls _service.IngestAsync twice
// FullSolutionIngestion benchmark calls _service.IngestAsync with forceFullReingest: true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale comments and fix audit frontmatter (QUAL-01, QUAL-02, QUAL-03)</name>
  <files>
    src/DocAgent.Indexing/InMemorySearchIndex.cs
    src/DocAgent.Indexing/KnowledgeQueryService.cs
    .planning/milestones/v1.2-phases/16-solution-mcp-tools/16-01-SUMMARY.md
  </files>
  <action>
1. In `src/DocAgent.Indexing/InMemorySearchIndex.cs`, delete the comment line at line 27 containing `// TODO: replace with BM25/inverted index`. Delete only that one comment line.

2. In `src/DocAgent.Indexing/KnowledgeQueryService.cs`, at line 215, replace the stale comment `// GetReferencesAsync (stub — MCPS-03, Phase 5/6 concern)` with a clean section header:
```csharp
// -------------------------------------------------------------------------
// GetReferencesAsync
// -------------------------------------------------------------------------
```

3. In `.planning/milestones/v1.2-phases/16-solution-mcp-tools/16-01-SUMMARY.md`, add `requirements_completed: [TOOLS-05]` to the YAML frontmatter. Check adjacent SUMMARY files for the correct field name and format to match.

4. Scan all `.cs` files under `src/` for additional stale TODO/FIXME/HACK comments. Per research, the only stale items are the two above — all other "stub" references are domain terminology (SymbolOrigin.Stub, stub nodes). Report scan results but make no additional removals.
  </action>
  <verify>
    <automated>cd /c/Development/CSharpDocAgentFrameworkMCP && grep -c "TODO.*BM25" src/DocAgent.Indexing/InMemorySearchIndex.cs; grep -c "stub.*MCPS-03" src/DocAgent.Indexing/KnowledgeQueryService.cs; grep -c "requirements_completed" .planning/milestones/v1.2-phases/16-solution-mcp-tools/16-01-SUMMARY.md</automated>
  </verify>
  <done>
    - grep for "TODO.*BM25" in InMemorySearchIndex.cs returns 0
    - grep for "stub.*MCPS-03" in KnowledgeQueryService.cs returns 0
    - grep for "requirements_completed" in 16-01-SUMMARY.md returns 1
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix benchmark wiring and register OTel meter (integration gaps)</name>
  <files>
    tests/DocAgent.Benchmarks/SolutionIngestionBenchmarks.cs
    src/DocAgent.McpServer/Program.cs
  </files>
  <action>
1. In `tests/DocAgent.Benchmarks/SolutionIngestionBenchmarks.cs`:
   - Change the field declaration at line 20 from `private SolutionIngestionService _service` to `private ISolutionIngestionService _service` (use the `ISolutionIngestionService` interface).
   - Update `GlobalSetup` (lines 35-38) to construct both services and wrap in the decorator:
     ```csharp
     var store = new SnapshotStore(_tempDir);
     var index = new InMemorySearchIndex();
     var fullLogger = LoggerFactory.Create(b => b.AddConsole())
         .CreateLogger<SolutionIngestionService>();
     var fullService = new SolutionIngestionService(store, index, fullLogger);
     var incrLogger = LoggerFactory.Create(b => b.AddConsole())
         .CreateLogger<IncrementalSolutionIngestionService>();
     _service = new IncrementalSolutionIngestionService(store, fullService, incrLogger);
     ```
   - Add `using` for `IncrementalSolutionIngestionService` if not already present (it lives in `DocAgent.McpServer.Ingestion` namespace).
   - The `FullSolutionIngestion` benchmark passes `forceFullReingest: true` which `IncrementalSolutionIngestionService` delegates through to the full service — no change needed to benchmark methods.

2. In `src/DocAgent.McpServer/Program.cs`, in the `.WithMetrics` lambda (lines 46-50), add `metrics.AddMeter("DocAgent.Ingestion");` between `AddRuntimeInstrumentation()` and `AddOtlpExporter()`. The meter name MUST be exactly `"DocAgent.Ingestion"` to match the `new Meter("DocAgent.Ingestion")` in `IncrementalSolutionIngestionService.cs`.

3. Run `dotnet build src/DocAgentFramework.sln` to verify no compilation errors.
  </action>
  <verify>
    <automated>cd /c/Development/CSharpDocAgentFrameworkMCP && dotnet build src/DocAgentFramework.sln && dotnet test --no-build</automated>
  </verify>
  <done>
    - dotnet build succeeds with zero errors
    - dotnet test passes all 303+ tests
    - SolutionIngestionBenchmarks._service is typed as ISolutionIngestionService
    - GlobalSetup constructs IncrementalSolutionIngestionService wrapping SolutionIngestionService
    - Program.cs contains metrics.AddMeter("DocAgent.Ingestion")
  </done>
</task>

</tasks>

<verification>
1. `grep "TODO.*BM25" src/DocAgent.Indexing/InMemorySearchIndex.cs` returns nothing
2. `grep "stub.*MCPS-03" src/DocAgent.Indexing/KnowledgeQueryService.cs` returns nothing
3. `grep "requirements_completed" .planning/milestones/v1.2-phases/16-solution-mcp-tools/16-01-SUMMARY.md` returns a line with TOOLS-05
4. `grep "ISolutionIngestionService" tests/DocAgent.Benchmarks/SolutionIngestionBenchmarks.cs` returns the field declaration
5. `grep "AddMeter.*DocAgent.Ingestion" src/DocAgent.McpServer/Program.cs` returns the registration line
6. `dotnet test` passes all tests
</verification>

<success_criteria>
- QUAL-01: InMemorySearchIndex.cs has no stale BM25 TODO
- QUAL-02: KnowledgeQueryService.cs has no stale stub label
- QUAL-03: 16-01-SUMMARY.md frontmatter includes requirements_completed: [TOOLS-05]
- IncrementalNoChange benchmark uses IncrementalSolutionIngestionService (not bare SolutionIngestionService)
- Program.cs registers metrics.AddMeter("DocAgent.Ingestion") for OTel counter collection
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-code-and-audit-cleanup/21-01-SUMMARY.md`
</output>
